{
  "name": "backend",
  "root": "apps/backend",
  "projectType": "application",
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn spring-boot:run",
        "cwd": "apps/backend"
      }
    }
  }
}
